version: 2.1
jobs:

  build:
    machine:
      image: ubuntu-1604:202007-01
    steps:
      - checkout
      - run:
          name: Run Database
          command: |
            docker-compose up -d postgres
      - run:
          name: Run Database
          command: |
            docker-compose up -d server
workflows:
    build:
      jobs:
        - build
