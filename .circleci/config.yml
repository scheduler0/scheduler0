version: 2.1
jobs:
  build:
    docker:
      - image: circleci/postgres:9.6.2-alpine
      - image: cimg/go:1.13

    steps:
      - checkout

      - run:
          name: Run tests
          command: |
            cd server
            go install
            go test ./...

workflows:
    build:
      jobs:
        - build
