version: 2.1
jobs:

  build:
    machine:
      image: ubuntu-1604:202007-01
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            docker-compose up

workflows:
    build:
      jobs:
        - build
